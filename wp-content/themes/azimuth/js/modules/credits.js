const info = { company : 'Azimuth', tagline : '"Stay the Course"',  version : '1.0' };

const init = () => {
  console.log( `${info.company} - ${info.tagline} - Version ${info.version}` );
  console.log( 'Site by Very Polite Agency – https://weareverypolite.com/' );
};

export default { init }

